% =====================================================================
%  Propagation operators on LEO ISL graphs: budget and t-parameterisation decide the ranking
%  Dich: Computer Communications (Elsevier, hybrid, KHONG gold OA)
%
%  ⛔ KHONG GO SO. Moi con so den tu ../figures/out/numbers.tex va tab*.tex, sinh boi
%     figures/make_figures.py tu code/results/grid12, grid3 va results-as-submitted.
%  ⛔ RANG BUOC TU claim-structure.md:
%   - KHONG "quantum-inspired" trong tieu de. KHONG chu "honest" o bat ky dau.
%   - Co che unitary la cua CTQW-GNN (arXiv:2608.20738); bai nay DO, khong nhan co che.
%   - TNSM dang phan bien: KHONG dung chung ma/du lieu/hinh, KHONG trich.
%   - Ban so bo 06/2026 chi duoc nhac nhu "preliminary version" kem repo, khong nhu bai da dang.
% =====================================================================
\documentclass[preprint,11pt,3p]{elsarticle}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{float}
\usepackage{multirow}
\usepackage{amsmath,amssymb}
\usepackage[hyphens]{url}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit}
\usepackage[hidelinks]{hyperref}

\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.6}
\renewcommand{\textfraction}{0.07}
\renewcommand{\floatpagefraction}{0.75}

\input{../figures/out/numbers.tex}

\newcommand{\Lap}{\mathbf{L}}
\newcommand{\Phiop}{\boldsymbol{\Phi}}

\journal{Computer Communications}

\begin{document}

\begin{frontmatter}

\title{Training Budget and Time Parameterisation Decide the Ranking of\\
Propagation Operators on LEO Inter-Satellite-Link Graphs}

\author[caira]{Phuc Hao Do\corref{cor1}}
\ead{haodp@dau.edu.vn}
\cortext[cor1]{Corresponding author.}
\affiliation[caira]{organization={CAIRA, Danang Architecture University},
  city={Da Nang}, country={Viet Nam}}

\begin{abstract}
A preliminary version of this study reported, from \numPrelimSeeds{} seeds, that the
transition-probability kernel of a continuous-time quantum walk, $|e^{-it\Lap}|^{2}$, outperforms
the heat kernel $e^{-t\Lap}$ as a propagation operator on a 1584-satellite low-Earth-orbit
inter-satellite-link graph. This paper is a reproducibility audit of that claim. Three operators
sharing one eigenbasis and one network are compared on Walker-delta shells of 264 to 4400
satellites, two node-field tasks and ten seeds, across arms that vary the parameterisation and
initialisation of the learned propagation time $t$, the capacity, the training budget and the
link pattern of the graph, against fixed-reach baselines whose reach is measured on the graph,
for \numTotalRuns{} plus \numRevTwoRuns{} training runs. Under the preliminary budget no operator
improves on a constant predictor by more than \numGainLargeMax\% on any shell of 1584 satellites
or more. With a five-fold budget every shell leaves the floor; on the torus the heat kernel is
better on the smallest shell on both tasks (ten seeds of ten, Holm-corrected
$p=\numPooledHopsSmallHolm$) and the paired tests certify no direction on the larger shells,
where their minimum detectable effect exceeds the observed differences. A per-cell test over
\numGthreeArmCells{} cells, the previous draft's headline, cannot reject in
\numPowerCannotReject{} of them at any effect size. Against the best tuned fixed-reach baseline a
learned $t$ is ahead by \numLadderGapBestPctHopsShellOne\% at 1584 satellites in its best arm
and behind in its worst. On the graph with the counter-rotating seam cut, which both versions
described and neither had run, the preliminary direction at 1584 satellites is recovered at the
corrected level and the rest of the grid is unchanged. Which long runs collapse to the constant
solution changes with the floating-point precision. All code, results and the dated protocol are
released.
\end{abstract}

\begin{keyword}
LEO constellations \sep inter-satellite links \sep graph neural networks \sep
spectral propagation \sep quantum walks \sep reproducibility \sep evaluation methodology
\end{keyword}

\end{frontmatter}

% =====================================================================
\section{Introduction}

The inter-satellite-link (ISL) graph of a low-Earth-orbit (LEO) mega-constellation is large and
nearly regular, and its links across the seam and the polar regions come and
go~\cite{plusgrid,starlink}. Learned models that propagate information over this graph have been
proposed for routing~\cite{lira,deepladu} and for connectivity decisions~\cite{comsat}. At each
layer such a model applies a propagation operator $\Phiop(\Lap)$ to node features and then a
shared nonlinearity~\cite{gcn,shuman}, so the choice of operator is a design decision whose
consequences grow with the graph.

Three operators span the design space studied here. The local step of a graph convolutional
network~\cite{gcn}, equation~\eqref{eq:gcn} below, moves information one hop per layer. The heat
kernel $e^{-t\Lap}$ moves it diffusively, over a range that grows like $\sqrt{t}$~\cite{grand}.
The transition-probability kernel of a continuous-time quantum walk~\cite{farhi},
$|e^{-it\Lap}|^{2}$, is a real, row-stochastic matrix whose rows spread ballistically, over a range
that grows like $t$, and it can be dropped into the same layer in place of the heat
kernel~\cite{qwnn,qgnn}. Recent work has made a separate and stronger argument for walk-based
operators: the unitary propagator $e^{-it\Lap}$ applied to features damps no frequency and
preserves feature norm, so it resists over-smoothing~\cite{ctqwgnn,ctqwformer,graphqwalk}. That
argument concerns the unitary operator in feature space. It is not tested here, and we say so at
the outset: the probability kernel studied in this paper is, like the heat kernel, a real
nonnegative row-stochastic matrix, and it inherits from the walk only the shape of its rows
(Section~\ref{sec:operators}). What the probability kernel might still offer is longer reach at
equal $t$, and whether that translates into a better propagation operator once $t$ is learned is
an empirical question.

A preliminary version of this study, circulated for review in June 2026 and available in the
companion repository, answered that question in the affirmative: on a 1584-satellite shell the
walk kernel reached a mean absolute error of \numPrelimWalkMAE{} against \numPrelimHeatMAE{} for the
heat kernel, from \numPrelimSeeds{} seeds, and the trend from smaller shells was read as the
ballistic mechanism emerging with scale. This paper is a reproducibility audit of that result. It
asks whether the ranking is a property of the two kernels or of the way they were evaluated, and
it answers by making explicit and varying the quantities the preliminary comparison left
implicit: the training budget relative to graph size, the parameterisation and initialisation of
the learned propagation time $t$, the capacity, the number of seeds, and the inferential rule by
which a winner is declared.

The contribution is the audit and what it finds, and what it adds to the correction of one
unpublished report is a method for reading such comparisons that is itself measured: the false-positive
rate of the vote rule the preliminary protocol used, the minimum effect the paired tests can
detect, the reach of every operator on the graph, and the sensitivity of a single run to the
arithmetic.
\begin{itemize}
\item A protocol in which the operators share one eigendecomposition, one network, one parameter
  count and one training loop; in which budget, $t$ parameterisation, $t$ initialisation,
  capacity and the link pattern of the graph are arms; in which fixed-reach decoupled propagation
  baselines, personalised PageRank and powers of the normalised adjacency, are run over a ladder
  whose reach is measured on the graph and bracket the learned reach of the kernels; and in
  which every reading rule is stated with its operating characteristics, including the
  minimum detectable effect of the corrected paired test, before a verdict is read
  (Section~\ref{sec:protocol}).
\item Under the preliminary budget of 200 epochs, no operator improves on the constant predictor by
  more than \numGainLargeMax\% on any shell of 1584 satellites or more, in any arm, at either
  capacity (Section~\ref{sec:floor}); the sensitivity of this statement to the floor threshold is
  tabulated. On the one shell where learning occurs the heat-versus-walk ranking changes with the
  $t$ arm, through a seed-level bimodality in which the two kernels escape the constant solution
  at indistinguishable rates (Section~\ref{sec:hidden}).
\item With a budget of 1000 epochs every shell leaves the floor. On the torus the heat kernel is
  better on the 264-satellite shell on both tasks (ten seeds of ten, Holm-corrected
  $p=\numPooledHopsSmallHolm$), and on the larger shells the paired tests certify no direction,
  with a minimum detectable effect that is stated and is larger than the observed differences
  (Section~\ref{sec:budget}). A per-cell test over \numGthreeArmCells{} arm-level cells, the
  first draft's headline, cannot reject in \numPowerCannotReject{} of them at any effect size and
  is reported as description. Against the best tuned fixed-reach baseline, a learned propagation
  time in its best arm is ahead by \numLadderGapBestPctHopsShellOne\% at 1584 satellites on the
  hop field and behind in its worst arm; the first draft's 53\% margin was measured against a
  baseline that had half-failed to train.
\item On the seam-cut graph, which both versions of this study said they used and neither had
  run, the preliminary direction at 1584 satellites on the hop field is recovered at the
  corrected level, while the other five (task, shell) pairs read as on the torus
  (Section~\ref{sec:seam}).
\item Long training collapses \numGridThreePathologyPct\% of runs to the constant solution,
  counted rather than dropped, and which runs collapse depends on the floating-point precision:
  the same seed escapes in one precision and collapses in the other
  (Section~\ref{sec:collapse}).
\item The released artifact of the preliminary version used a network of \numPrelimParams{}
  parameters while its released code defaults to \numReleasedParams{}, with no script for the
  experiment in question; and both versions misread the generator's seam flag, so that every
  main-grid run of this audit is on the full torus (Section~\ref{sec:threats}).
\end{itemize}

Section~\ref{sec:related} places the operators and the setting in the literature.
Section~\ref{sec:setting} defines the graphs (Section~\ref{sec:graphs}), the operators
(Section~\ref{sec:operators}) and the network and tasks (Section~\ref{sec:network}).
Section~\ref{sec:protocol} states the protocol and the reading rules (Section~\ref{sec:rules}).
Section~\ref{sec:results} reports the grids, the seam-cut arm (Section~\ref{sec:seam}) and
their cost (Section~\ref{sec:cost}).
Section~\ref{sec:discussion} interprets them, Section~\ref{sec:threats} lists what limits them, and
Section~\ref{sec:conclusion} concludes.

\section{Related work}
\label{sec:related}

\paragraph{Propagation operators and their reach}
Spectral graph convolutions began as polynomial filters of the Laplacian~\cite{chebnet,shuman};
the graph convolutional network of Kipf and Welling~\cite{gcn} keeps the first-order term and
applies a normalised adjacency once per layer, so depth bounds reach. Stacking layers drives
features toward a constant, the over-smoothing effect identified by Li et al.~\cite{lihanwu},
shown by Oono and Suzuki~\cite{oono} to be exponential in depth, and surveyed by Rusch et
al.~\cite{oversmooth}; remedies include decoupling propagation from transformation as in
SGC~\cite{sgc} and personalised-PageRank propagation~\cite{appnp}, and residual and identity
terms as in GCNII~\cite{gcnii}. Alon and Yahav~\cite{oversquash} describe the complementary
bottleneck by which information from exponentially many distant nodes is squashed into
fixed-size vectors, which Topping et al.~\cite{topping} relate to negative curvature of the
graph. Continuous-time views
replace the discrete step with a differential equation on the graph: GRAND~\cite{grand} uses
diffusion, whose solution is the heat kernel; PDE-GCN~\cite{pdegcn} and graph-coupled
oscillator networks~\cite{graphcon} use wave-type dynamics, whose reach grows linearly in
time rather than as a square root. The distinction between diffusive and ballistic spreading
that motivates the walk kernel in this paper is the same distinction, and the wave-type models
are its classical form.

\paragraph{Quantum walks as graph operators}
The continuous-time quantum walk of Farhi and Gutmann~\cite{farhi} evolves an amplitude
vector by $e^{-it\Lap}$ and spreads ballistically; Kempe~\cite{kempe} and M\"ulken and
Blumen~\cite{mulken} review the walk and its transport properties on complex networks, and
Childs~\cite{childs} shows it is computationally universal. Dernbach et al.~\cite{qwnn} and Verdon et
al.~\cite{qgnn} were among the first to place walk-derived operators inside trainable graph
models. GraphQWalk~\cite{graphqwalk} builds unsupervised, position-independent structural
embeddings from walk transition probabilities. CTQWformer~\cite{ctqwformer} learns the walk
Hamiltonian and feeds final-time probabilities into a graph transformer. CTQW-GNN~\cite{ctqwgnn}, a preprint at the time of writing,
uses the unitary propagator itself in feature space and proves that, because its eigenvalues lie
on the unit circle, no frequency is damped and the Dirichlet energy of the features cannot decay
exponentially with depth. That result concerns the unitary operator acting on features. The
operator studied here is the transition-probability kernel $|e^{-it\Lap}|^{2}$, a real
nonnegative matrix that shares the ballistic profile of the walk but not the norm-preserving
property; the distinction matters for interpreting the results
(Section~\ref{sec:discussion}). Quantum walks on communication networks have also been used
directly, for instance the Hadamard-walk model for network identification of
Liang et al.~\cite{hadamard}.

\paragraph{Graph learning on LEO constellations}
Handley~\cite{handley} first argued that inter-satellite routing in a mega-constellation is
a latency question, Bhattacherjee and Singla~\cite{plusgrid} fix the +Grid inter-satellite
topology that most later work assumes, Kassing et al.~\cite{hypatia} provide the Hypatia
simulator for such networks, and del Portillo et al.~\cite{starlink} compare the announced
constellations, including the Starlink shell whose geometry the 1584-satellite graph here
reproduces. Learned models on this graph include the link-identified routing architecture of
Zhang et al.~\cite{lira}, the duality-guided graph learning for joint connectivity and routing
of Gu et al.~\cite{deepladu}, and machine-learning-driven cellular-satellite
multi-connectivity~\cite{comsat}. None of these compares propagation operators under one training protocol
with the reach of every operator, learned or fixed, measured on the graph rather than asserted,
which is the gap this study addresses. The tasks used here are
deliberately decoupled from any particular routing or traffic-engineering system, so that the
propagation question is isolated from questions of load.

\paragraph{Evaluation pitfalls}
Shchur et al.~\cite{shchur} showed that the ranking of graph neural network architectures on
standard benchmarks changes with the data split and the seed, Henderson et al.~\cite{henderson}
made the corresponding point for deep reinforcement learning, where few seeds and unreported
tuning produce rankings that do not replicate, Bouthillier et al.~\cite{bouthillier} quantify
how much benchmark variance comes from sources other than the method, and Pineau et
al.~\cite{pineau} report on the reproducibility programme that turned such findings into
checklists. The present study applies the same discipline
to a comparison of propagation operators, and adds two quantities specific to spectral
operators, the training budget relative to graph size and the parameterisation of the
learned propagation time.

% =====================================================================
\section{Setting}
\label{sec:setting}

\subsection{Constellation graphs}
\label{sec:graphs}

Each graph is a snapshot of a Walker-delta shell. Satellite positions follow circular-orbit
propagation in an Earth-centred inertial frame; inter-satellite links follow the +Grid
pattern~\cite{plusgrid}, two intra-plane and two inter-plane links per satellite. The generator
suppresses inter-plane links above 70$^{\circ}$ latitude and can remove them across the
counter-rotating seam. In every run of the two main grids the seam links are \emph{kept}, and at
53$^{\circ}$ inclination no satellite reaches 70$^{\circ}$, so every snapshot of the main grids is
the full 4-regular torus of $P\times S$ satellites and $2N$ links; snapshots differ only in their
link lengths. The preliminary version and the first drafts of this paper stated that the seam was
cut; it was not, in either, and Section~\ref{sec:threats} records how the generator's flag was
misread. A seam-cut arm, in which the inter-plane links between the last and the first plane are
removed so that $2S$ satellites drop to degree three and the graph is no longer vertex-transitive,
is run at the larger budget and reported in Section~\ref{sec:seam}. The graph is restricted to its
largest connected component. Four shells are used (Table~\ref{tab:shells}). The 264-satellite shell is the main scale of the preliminary
version; the 1584-satellite shell reproduces the geometry of the first Starlink
shell~\cite{starlink}, 72 planes of 22 satellites at 53$^{\circ}$ and 550~km; the 3168- and
4400-satellite shells are scale-ups of that geometry, 72 planes of 44 and 100 planes of 44,
and are synthetic in the sense that no operator flies them. They exist to make the diameter
grow.

\begin{table}[htbp]
\centering
\caption{The four shells. All use +Grid links; the main grids keep the seam links, so each
snapshot is a 4-regular torus, and the seam-cut arm of Section~\ref{sec:seam} removes them. The
two largest are scale-ups of the Starlink shell-1 geometry and are not deployed constellations.}
\label{tab:shells}
\input{../figures/out/tab0-shells.tex}
\end{table}

Each training or evaluation instance draws a snapshot time uniformly over the orbital period
and a destination satellite uniformly over the component. Instance construction is seeded, so
every instance is reproducible from its index.

\subsection{Three operators on one eigenbasis}
\label{sec:operators}

Let $\Lap = \mathbf{D} - \mathbf{A}$ be the combinatorial Laplacian of the snapshot, with
eigendecomposition $\Lap = \mathbf{U}\,\mathrm{diag}(\lambda)\,\mathbf{U}^{\top}$ computed once
per instance and shared by all operators. The three operators are
\begin{align}
\Phiop_{\mathrm{gcn}} &= \tilde{\mathbf{D}}^{-1/2}(\mathbf{A}+\mathbf{I})\tilde{\mathbf{D}}^{-1/2}, \label{eq:gcn}\\
\Phiop_{\mathrm{heat}}(t) &= \mathbf{U}\,\mathrm{diag}\!\left(e^{-t\lambda}\right)\mathbf{U}^{\top}
  = e^{-t\Lap}, \label{eq:heat}\\
\Phiop_{\mathrm{walk}}(t) &= \left|\mathbf{U}\,\mathrm{diag}\!\left(e^{-it\lambda}\right)\mathbf{U}^{\top}\right|^{2}
  = \left|e^{-it\Lap}\right|^{2}, \label{eq:walk}
\end{align}
where the square in~\eqref{eq:walk} is taken entrywise on the modulus, so that
$[\Phiop_{\mathrm{walk}}]_{ij}$ is the probability that a continuous-time quantum walk started
at $j$ is found at $i$ after time $t$. This choice fixes what the study can and cannot say.
Both~\eqref{eq:heat} and~\eqref{eq:walk} are real, nonnegative and row-stochastic; applied to a
feature matrix, both average neighbouring features with nonnegative weights and both damp
high-frequency components. The unitary propagator $e^{-it\Lap}$ of CTQW-GNN~\cite{ctqwgnn} is
neither of these things, and its over-smoothing argument does not transfer to~\eqref{eq:walk}
and is not tested here. The two kernels differ in the profile of the row as a function of graph
distance, monotone for the heat kernel and oscillatory for the walk kernel, and in how that
profile's width grows with $t$; given that the network trains $t$, it is plausible from the
definitions alone that the two will be hard to tell apart, and the experiments below are the
test of that expectation, not of the unitary argument.

Figure~\ref{fig:spreading} makes the difference concrete on a path graph, where distance from
the source is a single coordinate. A row of the heat kernel is a bell whose spatial variance grows
as $t^{\numSpreadExpHeat}$; a row of the walk kernel is a pair of fronts whose variance grows as
$t^{\numSpreadExpWalk}$, with oscillations between them. The exponents are fitted from the
kernels themselves. This is the ballistic-versus-diffusive distinction in its cleanest form: at
equal $t$ the walk reaches farther, and its reach grows faster with $t$. Whether that translates
into a better propagation operator once $t$ is learned is the empirical question of this paper.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{../figures/out/fig5-spreading.pdf}
\caption{Analytic illustration on a path of 401 nodes. Left: one row of the heat kernel and of
the walk kernel from the central node at two propagation times, normalised to sum to one. Right:
spatial variance of the row against $t$ on logarithmic axes; the slopes fitted over
$t\in[\numSpreadFitTmin,\numSpreadFitTmax]$ are \numSpreadExpHeat{} for the heat kernel and
\numSpreadExpWalk{} for the walk kernel, with root-mean-square log residuals of
\numSpreadResidHeat{} and \numSpreadResidWalk{}.}
\label{fig:spreading}
\end{figure}

\subsection{Network, parameterisation of $t$, and tasks}
\label{sec:network}

The network has $K$ layers, each computing
$\mathbf{H}^{(\ell+1)} = \sigma\!\left(\Phiop^{(\ell)}\,\mathbf{H}^{(\ell)}\mathbf{W}^{(\ell)}\right)$
with a ReLU nonlinearity and a linear read-out to one scalar per node. For the two spectral
operators, each layer carries its own scalar propagation time $t_{\ell}$, trained jointly with
the weights. The preliminary version parameterised it as $t_{\ell}=\mathrm{relu}(\theta_{\ell})$
with $\theta_{\ell}$ initialised at 0.5. Section~\ref{sec:protocol} explains why this is an arm
of the present study and adds $t_{\ell}=\mathrm{softplus}(\theta_{\ell})$, which keeps
$t_{\ell}>0$ with a live gradient, initialised so that the two parameterisations start at the
same value. The GCN operator has no $t$.

The input to every node is two features, an indicator of the destination and the normalised
degree. Two node-field targets are used. With $d$ the destination, $h(i,d)$ the geodesic hop count
and $\tau(i,d)$ the shortest-path propagation delay computed on the inter-satellite link
lengths, the targets are
\begin{equation}
y^{\mathrm{hop}}_{i} = \frac{h(i,d)}{\max_{j} h(j,d)}, \qquad
y^{\mathrm{delay}}_{i} = \frac{\tau(i,d)}{\max_{j} \tau(j,d)},
\label{eq:tasks}
\end{equation}
so that both lie in $[0,1]$ and the constant predictor has the same meaning on every shell. The
hop field is the task of the preliminary version. Both fields in~\eqref{eq:tasks}
require the model to move a single seeded signal across the graph, which is the propagation
question in isolation; neither involves traffic or load. The metric is the mean absolute
error (MAE) over nodes on the evaluation instances of a seed, which are drawn from the same
generator as its training instances with a different destination and snapshot time.

One property of the torus must be stated here because it decides what the evaluation instances
measure. A torus is vertex-transitive, so the hop field from any destination is the same field up
to a relabelling of the nodes, and the network, whose only inputs are the destination indicator
and the (constant) normalised degree, is equivariant to that relabelling. On the hop field the
evaluation instances of the main grids are therefore copies of the training instances, and the
training error equals the test error exactly (Table~\ref{tab:fit}); the hop-field task on the
torus is a fitting problem on one field, not a generalisation problem. The delay field depends on
the link lengths, which vary with the snapshot, and differs between instances. On the seam-cut
graph the destination's position relative to the seam changes the hop field, and the instances
differ on both tasks (Section~\ref{sec:seam}).

% =====================================================================
% =====================================================================
\section{Protocol}
\label{sec:protocol}

\subsection{Parameter matching and the arms}

The operators share depth, width, weight shapes and training loop, so a difference in error is
attributable to the operator and not to capacity. Two capacities are used: width 16 with three
layers, the default of the released code of the preliminary version (\numReleasedParams{}
parameters including the $t_{\ell}$), and width 32 with four layers, which the text of the
preliminary version describes and its released results were produced with
(\numSubmittedParams{} parameters). Adam~\cite{adam} at learning rate 0.01, eight training and six
evaluation instances per seed, and full-batch updates are used throughout. The six evaluation
instances of a seed are drawn with that seed's own generator, so the ten seeds of a cell evaluate
on sixty distinct snapshot-destination pairs; seed-to-seed dispersion therefore includes
instance-to-instance variation.

Four quantities are arms rather than fixed choices.
\begin{itemize}
\item \textbf{Parameterisation of $t$.} A pilot run under the released parameterisation showed the
  learned $t_{\ell}$ going negative in the second and third layers, where
  $\mathrm{relu}(t_{\ell})=0$ turns the propagator into the identity and stops its gradient, so
  that a model reported as three or four layers of spectral propagation was propagating in one.
  The softplus parameterisation removes this; the relu parameterisation is kept as a control so
  that the preliminary configuration is one of the arms.
\item \textbf{Initialisation of $t$.} $t_{0}\in\{0.5, 2\}$, since the same pilot showed the
  learned reach, and with it the operator ranking, depending on where $t$ starts.
\item \textbf{Capacity.} The two configurations above.
\item \textbf{Budget.} 200 epochs, the preliminary value, held fixed across scales; and 1000
  epochs for the second grid.
\item \textbf{Graph.} The torus of the main grids, and the seam-cut graph, run at 1000 epochs in
  all four $t$ arms (Section~\ref{sec:seam}).
\end{itemize}
Grid 1 runs both capacities, all four $t$ arms, four shells, two tasks, three operators and ten
seeds at 200 epochs (\numGridTwelveRuns{} runs). Grid 2 runs the width-32 configuration at 1000
epochs on the three smaller shells with \numGridThreeArms{} $t$ arms (\numGridThreeRuns{} runs).
With the fixed-reach baselines below (\numFixedRunsTwelve{} runs at 200 epochs and
\numFixedRunsThree{} at 1000), the two main grids comprise \numTotalRuns{} training runs; the
baseline ladder, the seam-cut arm and the precision check of Section~\ref{sec:collapse} add
\numLadderRuns{}, \numSeamRuns{} and \numPrecRuns{}. All runs were executed on one machine, one
GPU per arm, with the eigendecomposition of each instance cached in single precision and shared,
so that every arm sees bit-identical eigenpairs. Whether single precision is adequate is not
settled by that identity; it is tested directly by rerunning one cell entirely in double precision
(Section~\ref{sec:collapse}).

\paragraph{Fixed-reach decoupled baselines, and their measured reach}
A comparison of the three operators above confounds ``spectral'' with ``long-reaching'': the GCN
step reaches one hop per layer, the spectral kernels reach as far as their learned $t$ allows.
Two further operators, run at both budgets on the width-32 configuration, separate the two. Both
decouple propagation from transformation in the sense of SGC~\cite{sgc} and personalised-PageRank
propagation~\cite{appnp}, have fixed reach and no learned $t$, and use the same network:
\begin{equation}
\Phiop_{\mathrm{PPR}} = \alpha \sum_{k=0}^{K} (1-\alpha)^{k}\,\hat{\mathbf{A}}^{k},\quad
\alpha=\numPprAlpha,\ K=\numPprK;\qquad
\Phiop_{\mathrm{SGC}} = \hat{\mathbf{A}}^{K'},\quad K'=\numSgcK,
\label{eq:fixed}
\end{equation}
with $\hat{\mathbf{A}}$ the normalised adjacency of~\eqref{eq:gcn} and $\Phiop_{\mathrm{PPR}}$
renormalised to row sums of one. The values of $\alpha$, $K$ and $K'$ in~\eqref{eq:fixed} were
fixed by assertion in the first draft, on the reasoning that $1/\alpha$ and $K'$ hops are ``of the
order of'' the learned reach. That reasoning confuses the support of a propagator with where its
mass sits: on a lattice, $K'$ steps of a normalised adjacency place their mass at about
$\sqrt{K'}$ hops, not $K'$. Reach is therefore measured, not asserted. For one row of each
propagator, Table~\ref{tab:reach} reports the expected hop distance from the source under the
row's probability mass, per layer at the learned $t$ of Table~\ref{tab:tlearned} for the two
kernels and composed over the four layers. At 1584 satellites the heat kernel's layers reach
\numReachHeatLayerOneShellOne{} to \numReachHeatMeanShellOne{} hops on average and the walk
kernel's up to \numReachWalkLayerFourShellOne{}, while the baselines of~\eqref{eq:fixed} reach
\numReachPprRoundOneShellOne{} (PPR) and \numReachSgcEightShellOne{} (SGC) hops per layer: the
first-draft baselines reached about half as far as the kernels they were said to match, and the
truncation at $K=\numPprK$ steps caps the PPR reach whatever $\alpha$ says. The comparison is
therefore run over a ladder that brackets the learned reach on every shell: SGC with
$K'\in\{2,8,32,64\}$ and PPR computed exactly, $\alpha(\mathbf{I}-(1-\alpha)\hat{\mathbf{A}})^{-1}$,
with $\alpha\in\{0.2,0.05,0.02,0.01\}$, whose per-layer reach runs from 1.3 to
\numReachPprExactOneDouble{} hops (Table~\ref{tab:reach}). Every rung is run at 1000 epochs, three
shells, both tasks, ten seeds (\numLadderRuns{} runs), and the best rung per (task, shell) is the
baseline the spectral kernels are compared with in Section~\ref{sec:budget}; the first-draft
values are kept in the ladder and in Table~\ref{tab:fixed} so that their result remains
checkable.

\begin{table}[htbp]
\centering
\caption{Measured reach: expected hop distance from the source under one row of each propagator,
on the torus of each shell. For the two kernels the reach is evaluated at the learned $t$ of
Table~\ref{tab:tlearned}, layer by layer and composed over the four layers; for the fixed-reach
operators it is a property of $\alpha$ or $K'$ alone. The first-draft baselines
of~\eqref{eq:fixed} are the rows marked round 1.}
\label{tab:reach}
\input{../figures/out/tab-reach.tex}
\end{table}

\subsection{Reading rules, declared before the runs, with their operating characteristics}
\label{sec:rules}

Six rules govern how a cell, one (task, shell, arm) combination, is read. The first five were
fixed before the first grid was launched, and two of them were tightened after the first runs
exposed a gap; both changes are dated in the repository and stated here. The sixth was added
after external review of a draft showed that the fourth, as stated, had a false-positive rate the
draft never computed, and its unit of inference was changed after a second review showed that
the first form of the rule had, by construction, almost no power; both forms are reported.
\begin{enumerate}
\item \textbf{Constant baseline.} Every cell reports, on every metric it is scored on, the error
  of the predictor that outputs the mean of the target, in the tables themselves. Whether an
  operator beats it is a result, never a filter.
\item \textbf{Pathology.} A run is excluded only for a training pathology: a non-finite loss, or
  a loss that rises by more than 1\% between successive thirds of training. Excluded runs are
  counted next to every number they affect, and Section~\ref{sec:collapse} reports whether each
  excluded run had left the constant solution before collapsing, since exclusion after collapse
  removes non-escaping runs preferentially. A run whose loss is flat from the start because it
  sits on the constant solution is a \emph{plateau}, a legitimate outcome, and is kept. The first
  draft of this rule required a 10\% loss drop and would have discarded plateaus as failures; it
  was corrected when the first grid produced them.
\item \textbf{Floor.} Let $\bar{e}_{\mathrm{op}}$ be the mean MAE of an operator over its valid
  seeds in a cell and $\bar{e}_{0}$ that of the constant predictor. The cell's gain is
  \begin{equation}
  g = \frac{\bar{e}_{0} - \min_{\mathrm{op}} \bar{e}_{\mathrm{op}}}{\bar{e}_{0}},
  \label{eq:gain}
  \end{equation}
  and the cell is \emph{rankable} only if $g \geq 0.05$. This threshold was chosen after the
  first completed cells, all on the 4400-satellite shell, showed the three operators' means
  within \numLargeSpreadMax{} of one another and of the constant baseline while the majority rule
  below still declared a winner. Because the value was set with that knowledge, every statement
  that depends on it is repeated at $g\in\{0.01, 0.02, 0.05, 0.10\}$ in Section~\ref{sec:sens}.
\item \textbf{Seed majority.} Within a rankable cell, with $S$ the set of seeds valid for both
  spectral operators and $e_{\mathrm{op},s}$ the MAE of seed $s$, the heat kernel wins the
  majority if
  \begin{equation}
  \bar{e}_{\mathrm{heat}} < \bar{e}_{\mathrm{walk}}
  \quad\text{and}\quad
  \bigl|\{ s \in S : e_{\mathrm{heat},s} < e_{\mathrm{walk},s} \}\bigr| \geq 7,
  \label{eq:win}
  \end{equation}
  and symmetrically for the walk kernel; otherwise the cell is a tie, and fewer than seven valid
  seed pairs leaves it undecided. This is the rule of the preliminary protocol, and it is kept so
  that the preliminary verdicts can be reproduced. Its operating characteristics are the point:
  under the null of exchangeable operators, $P(X\geq 7 \mid n=10, p=\tfrac12) = 0.172$, so a
  winner is declared in 34.4\% of null cells, and the two-sided sign-test $p$-values behind a
  7:3, 8:2 and 8:1 majority are 0.344, 0.109 and 0.039. A majority verdict is therefore reported
  beside, never instead of, the tests of rule~6, and every table that prints a verdict also
  prints which operator led in mean, so that a ``tie'' cannot hide a lead.
\item \textbf{Robustness.} A ranking at a (task, shell) is claimed only if it is the same in
  every $t$ arm run at that budget. Grid 2 runs \numGridThreeArms{} arms, and the number is
  stated wherever the rule is applied.
\item \textbf{Inference.} The unit of inference is the (task, shell) pair, of which there are
  six at the larger budget. For each seed $s$, the per-seed difference
  $d_{s}=e_{\mathrm{heat},s}-e_{\mathrm{walk},s}$ is averaged over the $t$ arms in which seed $s$
  is valid for both kernels, giving one paired difference per seed; the ten values are summarised
  by their mean with a 95\% bootstrap confidence interval, by the exact two-sided sign test and by
  the exact two-sided Wilcoxon signed-rank test, and the Wilcoxon $p$-values are Holm-corrected
  over the six pairs of the same metric. A difference between the two kernels is claimed at a
  (task, shell) only where the corrected $p$ is below 0.05. The same statistics are also reported
  for every arm-level cell, with Holm correction over the \numGthreeArmCells{} cells, as the
  first draft did; these per-cell tests are descriptive, for the reason given next. The tests were
  checked against an independent implementation on random data and agree to nine decimal places.
\end{enumerate}

\paragraph{Operating characteristics of rule~6}
The exact Wilcoxon test on $n$ non-zero pairs cannot return a two-sided $p$ below $2/2^{n}$, and
Holm over $m$ tests requires the smallest $p$ to fall below $0.05/m$. Table~\ref{tab:power} applies
this to the arm-level cells of grid~2: with $m=\numGthreeArmCells$ the threshold is
$\numHolmThreshold$, which $n=9$ pairs cannot reach ($2/2^{9}=\numPowerMinPNine$) and $n=10$ pairs
reach only on a unanimous $10{:}0$ split ($\numPowerMinPTen$). Of the \numGthreeArmCells{} cells,
\numPowerCannotReject{} have nine or fewer valid pairs after exclusions and cannot reject at any
effect size, and the remaining \numPowerNeedSweep{} require every seed to agree. A ``0 of
\numGthreeArmCells{}'' under that rule is close to a property of the design, which is why the
first draft's headline, which reported exactly that, was withdrawn. The table also gives, for
each $|S|$, the minimum detectable effect (MDE) at the uncorrected 0.05 level and 80\% power,
computed by simulation from each cell's own seed dispersion. Pooling over arms gives every
(task, shell) pair its full ten seeds and reduces $m$ to 6, so the threshold becomes
$\numPooledThreshold$ and a $10{:}0$ or a favourable $9{:}1$ split can reject. The MDE of the
pooled test at the corrected level is reported beside every pooled result
(Table~\ref{tab:pooled}); it is what the design can resolve, and where the observed difference is
smaller than it the correct statement is that the design cannot certify a difference, not that
none exists. Dependence among the tests is stated rather than assumed away: the four arms at a
(task, shell) share instances, seeds and eigenbases, which is why they are pooled into one test,
and the two tasks of a shell share graphs and seeds, so the six pooled tests are not independent.
Holm's procedure controls the family-wise error rate under arbitrary dependence, so the correction
remains valid; it is conservative, not invalid, under this structure.

\begin{table}[htbp]
\centering
\caption{Operating characteristics of the per-cell form of rule~6 on grid~2, MAE. For each
number of valid seed pairs $|S|$: the smallest two-sided $p$ the exact Wilcoxon test can return,
that value times the \numGthreeArmCells{} cells of the Holm family, whether any outcome can
survive the correction, how many cells have that $|S|$, and the median over those cells of the
minimum detectable effect at $\alpha=0.05$ (uncorrected) and 80\% power, in MAE units, from each
cell's own seed dispersion.}
\label{tab:power}
\input{../figures/out/tab-power.tex}
\end{table}
All six rules are implemented once, in the analysis script that produces every table and figure,
and the same script prints the pathology and plateau counts beside every mean.
Figure~\ref{fig:protocol} summarises the design, and Table~\ref{tab:summary} collects the headline
numbers with the section that derives each.

\begin{figure}[htbp]
\centering
\input{../figures/out/fig-protocol.tex}
\caption{The study design. Every cell of the grid, one (task, shell, arm), runs the operators on
the same instances and seeds; the reading rules are applied in order by one script. A majority
verdict (rule~4) is reported beside the paired tests of rule~6, never instead of them.}
\label{fig:protocol}
\end{figure}

\begin{table}[htbp]
\centering
\caption{Headline numbers and where each is derived. Every value is generated from the result
files by the same script that produces the figures.}
\label{tab:summary}
\input{../figures/out/tab-summary.tex}
\end{table}

% =====================================================================
\section{Results}
\label{sec:results}

\subsection{A fixed budget produces a floor at scale}
\label{sec:floor}

Figure~\ref{fig:floor} and Table~\ref{tab:floor} show, for each capacity and each $t$ arm, the
gain~\eqref{eq:gain} of the best operator over the constant predictor as the shell grows, at the
preliminary budget of 200 epochs. On the 264-satellite shell the gain on the hop field is between
\numGainHopsSmallMinReleased\% and \numGainHopsSmallMaxReleased\% at the released capacity and
between \numGainHopsSmallMinSubmitted\% and \numGainHopsSmallMaxSubmitted\% at the submitted
capacity; on the delay field it is between \numGainDelaySmallMinReleased\% and
\numGainDelaySmallMaxReleased\% and between \numGainDelaySmallMinSubmitted\% and
\numGainDelaySmallMaxSubmitted\%. On every larger shell it never exceeds \numGainLargeMax\%, in any
of the eight capacity-by-arm combinations, on either task. Of the \numFloorCellsLarge{} hop-field
cells at 1584 satellites and above, \numRankableCellsLarge{} are rankable at the declared 5\% floor;
Section~\ref{sec:sens} shows that \numRankableLargeAtGTwo{} become rankable at a 2\% floor and
\numRankableLargeAtGOne{} at 1\%, all at 1584 satellites, and Table~\ref{tab:above} lists the
\numGridTwelveAbove{} of \numGridTwelveLargeRuns{} valid large-shell runs that end more than 2\%
below the constant predictor: they concentrate at 1584 satellites, the walk kernel contributes
\numAboveWalkShellOne{} of them and the heat kernel \numAboveHeatShellOne{}, and the largest
single-run gain among them is \numAboveMaxGainPct\%. Of \numGridTwelveRuns{} runs,
\numGridTwelvePathology{} were excluded as pathologies and \numGridTwelvePlateau{} were plateaus in
the sense of rule~2, kept.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{../figures/out/fig1-floor.pdf}
\caption{Gain~\eqref{eq:gain} of the best operator over the constant predictor at 200 epochs,
as a function of shell size, for the hop field (top) and the delay field (bottom), the released
and the submitted capacity (columns) and the four $t$ arms. The dotted line is the 5\% floor of
rule~3. Above 264 satellites every combination lies below it on both tasks.}
\label{fig:floor}
\end{figure}

\begin{table}[htbp]
\centering
\caption{Gain (\%) of the best operator over the constant predictor at 200 epochs, ten seeds
per cell, both tasks. A dagger marks cells below the 5\% floor, where the operator ranking is
not read.}
\label{tab:floor}
\input{../figures/out/tab1-floor.tex}
\end{table}

\begin{table}[htbp]
\centering
\caption{Large-shell runs at 200 epochs whose error is more than 2\% below the constant
predictor, by capacity, operator and shell.}
\label{tab:above}
\input{../figures/out/tab-above.tex}
\end{table}

Doubling the width and adding a layer does not change this: the submitted capacity is as far
below the floor at 1584 satellites as the released one. Nor does the $t$ arm. The floor is
therefore not a capacity effect and not a parameterisation effect; Section~\ref{sec:budget} shows
that it is a budget effect.

\subsection{The propagation time is a hidden variable}
\label{sec:hidden}

On the 264-satellite shell, where learning does occur at 200 epochs, the ranking between the two
spectral operators is not stable. Figure~\ref{fig:arms} plots every seed for the submitted
capacity under the four $t$ arms; the majority verdicts read, in arm order,
\numSTwoSixFourVerdictsSubmitted{}. Table~\ref{tab:s264} gives the means, seed counts, the
operator leading in mean, the sign-test $p$ and the verdict for all eight capacity-by-arm
combinations, including the released capacity, where they read
\numSTwoSixFourVerdictsReleased{}. Of the \numSTwoSixFourCells{} verdicts, \numSTwoSixFourTies{}
are ties, \numSTwoSixFourHeatWins{} are heat majorities and \numSTwoSixFourWalkWins{} is a walk
majority, the latter at $p=\numSTwoSixFourWalkWinP$; no verdict repeats across all four arms at
either capacity, and no cell reaches $p<0.05$ on the sign test. The same operators on the same
instances rank differently depending only on the $t$ arm, and none of the rankings is
distinguishable from noise.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{../figures/out/fig2-s264-arms.pdf}
\caption{Per-seed MAE on the hop field, 264 satellites, submitted capacity, 200 epochs, for
the four $t$ arms. Bars are means over valid seeds; the dotted line is the constant predictor.
The two spectral operators are bimodal across seeds: a run either leaves the constant solution
or stays near it.}
\label{fig:arms}
\end{figure}

\begin{table}[htbp]
\centering
\caption{Hop field, 264 satellites, 200 epochs: MAE over valid seeds (mean $\pm$ sd for the two
spectral operators, mean for the GCN, valid-seed count in parentheses; the GCN's sd is
\numGcnSdSmall{} or less in every cell) under each capacity and $t$ arm, the number of seeds on which each spectral operator is lower, the two-sided sign-test $p$,
and the majority verdict. The constant predictor scores \numSTwoSixFourConst{}.}
\label{tab:s264}
\input{../figures/out/tab3-s264-200ep.tex}
\end{table}

The figure shows why. For both spectral operators the ten seeds do not scatter around a mean;
they split. Some runs reach errors well below the constant predictor and the rest stay near it.
Table~\ref{tab:escape} counts the escapes, defined as a seed whose error is below 90\% of the
constant predictor's. Pooled over the eight combinations, \numEscapeHeat{} heat runs
(\numEscapeHeatPct\%) and \numEscapeWalk{} walk runs (\numEscapeWalkPct\%) escape; a
two-proportion test gives $p=\numEscapePz$. If the escape process is a coin flip with the same
bias for both operators, which is what these rates say, then every verdict in Table~\ref{tab:s264}
is a difference of two or three seeds out of ten, and a mean over \numPrelimSeeds{} seeds at this
budget, the preliminary version's sample, is the mean of a coin flip. The left panel of
Figure~\ref{fig:tlearned} shows the learned $t_{\ell}$ per layer at this budget: the first layer
learns a larger $t$ than the later ones for both operators, and the values do not grow with the
shell, consistent with the models on large shells not having moved from the constant solution.

\begin{table}[htbp]
\centering
\caption{Seeds that escaped the constant solution (MAE below 90\% of the constant predictor)
out of valid seeds, hop field, 264 satellites, 200 epochs.}
\label{tab:escape}
\input{../figures/out/tab4-escape.tex}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{../figures/out/fig3-t-learned.pdf}
\caption{Learned propagation time per layer (lighter to darker: first to fourth layer) against
shell size, hop field, submitted capacity, softplus arm with $t_{0}=0.5$, mean over valid seeds.
Left: 200 epochs (grid 1). Right: 1000 epochs (grid 2). Note the different vertical scales.}
\label{fig:tlearned}
\end{figure}

\subsection{With enough budget, the heat kernel leads on the smallest shell and neither kernel is resolved on the larger ones}
\label{sec:budget}

Grid 2 raises the budget to 1000 epochs at the submitted capacity on the three smaller shells,
with \numGridThreeArms{} $t$ arms. Every cell leaves the floor on both metrics: on MAE the best
operator improves on the constant predictor by \numGthreeHopsGainMin\% to \numGthreeHopsGainMax\%
on the hop field and by \numGthreeDelayGainMin\% to \numGthreeDelayGainMax\% on the delay field,
depending on shell and arm (Table~\ref{tab:grid3}), and on the far-node metric, against the
constant predictor's own far-node error (\numConstAurcHopsShellOne{} at 1584 satellites on the hop
field, against \numConstMaeHopsShellOne{} on MAE), by \numAurcGainLargeMin\% to
\numAurcGainLargeMax\% on the two large shells (Table~\ref{tab:aurc}); \numAurcFloorCells{} of the
\numGthreeArmCells{} far-node cells are at the floor. The floor of Section~\ref{sec:floor} was a
budget floor. Table~\ref{tab:tlearned} and the right panel of Figure~\ref{fig:tlearned} show what
the budget buys: the heat kernel's first-layer $t$ grows with the shell, from
\numTlayerOneSmallHeat{} at 264 satellites to \numTlayerOneShellOneHeat{} at 1584 and
\numTlayerOneDoubleHeat{} at 3168, while the walk kernel's stays between
\numTlayerOneSmallWalk{} and \numTlayerOneDoubleWalk{}. The diffusive operator reaches farther on
larger graphs by learning to.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{../figures/out/fig4-scale-1000ep.pdf}
\caption{MAE against shell size at 1000 epochs, submitted capacity, the two $t_{0}=0.5$ arms,
both tasks, with the reach-matched PPR and SGC baselines where run. Points are means over valid
seeds and bars one standard deviation; the dotted line is the constant predictor. Points are
offset horizontally within each shell for legibility.}
\label{fig:scale}
\end{figure}


\begin{table}[htbp]
\centering
\caption{Grid 2, MAE at 1000 epochs, submitted capacity: the constant predictor's MAE, then
mean $\pm$ sd over valid seeds with the count in parentheses for each operator, the operator
leading in mean, the number of seeds on which each spectral operator is lower, and the majority
verdict of rule~4. ``n$<$7'' marks a cell with too few valid seed pairs for that rule.}
\label{tab:grid3}
\input{../figures/out/tab2-grid3.tex}
\end{table}

\begin{table}[htbp]
\centering
\caption{Learned propagation time per layer at 1000 epochs, hop field, submitted capacity,
softplus arm with $t_{0}=0.5$, mean over valid seeds.}
\label{tab:tlearned}
\input{../figures/out/tab7-tlearned.tex}
\end{table}

\begin{table}[htbp]
\centering
\caption{Grid 2, MAE: paired inference across seeds for each arm-level cell, 1000 epochs,
submitted capacity (the descriptive, per-cell form of rule~6). $\bar d$ is the mean over valid
seed pairs of the per-seed difference $e_{\mathrm{heat}}-e_{\mathrm{walk}}$, negative favouring
the heat kernel, with its 95\% bootstrap interval; the three $p$-values are the exact two-sided
sign test, the exact two-sided Wilcoxon signed-rank test, and the Wilcoxon $p$ Holm-corrected
over the \numGthreeArmCells{} cells of this table. Table~\ref{tab:power} shows that
\numPowerCannotReject{} of these cells cannot reach a corrected $p$ below 0.05 at any effect
size; the constant predictor for each cell is in Table~\ref{tab:grid3}.}
\label{tab:grid3inf}
\input{../figures/out/tab2b-grid3-inference.tex}
\end{table}

\begin{table}[htbp]
\centering
\caption{Grid 2, MAE: the pooled form of rule~6, one test per (task, shell). For each seed the
difference $e_{\mathrm{heat}}-e_{\mathrm{walk}}$ is averaged over the $t$ arms in which that seed
is valid for both kernels; the columns give the mean of the ten per-seed values with its 95\%
bootstrap interval, the exact sign-test and Wilcoxon $p$, the Wilcoxon $p$ Holm-corrected over
the six rows, the seed vote, and the minimum detectable effect of this test at the corrected
level ($0.05/6$) and 80\% power from the row's own seed dispersion, in MAE units. An asterisk
marks a corrected $p$ below 0.05.}
\label{tab:pooled}
\input{../figures/out/tab-pooled.tex}
\end{table}


Figure~\ref{fig:scale} and Tables~\ref{tab:grid3} to~\ref{tab:pooled} give the comparison.
Read by the pooled test of rule~6 (Table~\ref{tab:pooled}), which is the reading this paper
stands on, the heat kernel is better on the 264-satellite shell on both tasks: all ten seeds
favour it on the hop field ($\bar d=\numPooledHopsSmallD$, Holm-corrected
$p=\numPooledHopsSmallHolm$) and on the delay field ($\bar d=\numPooledDelaySmallD$,
$p=\numPooledDelaySmallHolm$). On the two larger shells no direction is certified: the seed votes
are \numPooledHopsShellOneWins{} and \numPooledHopsDoubleWins{} on the hop field and
\numPooledDelayShellOneWins{} and \numPooledDelayDoubleWins{} on the delay field, the last two
in opposite directions, with corrected $p$ of \numPooledHopsShellOneHolm{},
\numPooledHopsDoubleHolm{}, \numPooledDelayShellOneHolm{} and \numPooledDelayDoubleHolm{}. The
minimum detectable effect of these four tests is between \numPooledMdeLargeMin{} and
\numPooledMdeMax{} in MAE units, against constant-predictor errors near 0.2; at 1584 satellites on
the hop field, where the preliminary claim was made, the design resolves a difference of
\numPooledHopsShellOneMde{} and the observed difference is \numPooledHopsShellOneD{}. The
per-cell statistics (Table~\ref{tab:grid3inf}) describe the same picture: \numCIExclZero{} of the
\numGthreeArmCells{} bootstrap intervals exclude zero, \numCIExclZeroHeat{} of them favouring the
heat kernel and \numCIExclZeroWalk{} the walk kernel, against \numCIExpectedNull{} expected under
a null at 95\% coverage, and \numCIExclZeroSmall{} of the \numCIExclZero{} are on the 264-satellite
shell; all \numGthreeSigRaw{} cells with uncorrected Wilcoxon $p<0.05$ favour the heat kernel;
and \numGthreeSigHolm{} cells survive the per-cell Holm correction, which Table~\ref{tab:power}
shows \numPowerCannotReject{} of them could not have done at any effect size. Read by the
majority rule~\eqref{eq:win} of the preliminary protocol, the heat kernel wins in every arm at
\numGthreeHeatRobust{} of \numGthreeCells{} task-scale cells, both of them the 264-satellite
shell, and the walk kernel at \numGthreeWalkRobust{}; by mean the heat kernel leads in
\numGthreeLeadHeat{} and the walk kernel in \numGthreeLeadWalk{} of the \numGthreeArmCells{}
arm-level cells. The ratio of the walk kernel's seed standard deviation to the heat kernel's has
median \numSdRatioMedian{} over the \numSdRatioCells{} cells, exceeds 3 in \numSdRatioGeThree{}
and is below 1 in \numSdRatioLtOne{}; at ten seeds the relative standard error of a standard
deviation is about 24\%, so no statement about relative variance is made. The preliminary
version's finding, a walk-kernel error of \numPrelimWalkMAE{} against \numPrelimHeatMAE{} at 1584
satellites, is not reproduced on the torus: under the same parameterisation and capacity, with
ten seeds instead of \numPrelimSeeds{}, the seed vote at that scale is
\numPooledHopsShellOneWins{} and the only certified direction in the grid, on the smallest shell,
is the opposite one. Whether it reproduces on the seam-cut graph is the question of
Section~\ref{sec:seam}.

The local GCN operator is the worst everywhere, as expected for a four-hop model on graphs whose
eccentricity is tens of hops; on the hop field it clears the 5\% floor only on the 264-satellite
shell (gain \numGthreeGcnGainSmall\%, against \numGthreeGcnGainShellOne\% at 1584 and
\numGthreeGcnGainDouble\% at 3168). Table~\ref{tab:fit} places training error beside test error
for each operator. On the hop field the two are equal to five decimals in every cell (largest
difference \numFitMaxGapHops), and on the delay field they differ by at most \numFitMaxGap{}: this
is the vertex-transitivity of Section~\ref{sec:network} made visible. No model overfits, every
model underfits, and on the torus the evaluation instances carry no generalisation information
that the training instances do not; the errors of this grid measure how well each operator fits
one field, and the comparison between operators is unaffected because all are fitted to the
same one. On the seam-cut graph, where the instances differ, training and test error separate
by up to \numSeamFitMaxGap{} (Section~\ref{sec:seam}).

\begin{table}[htbp]
\centering
\caption{Training and test MAE at 1000 epochs, submitted capacity, softplus arm with
$t_{0}=0.5$, mean over valid seeds, on the torus. The training column is the error on the eight
instances each model was fitted to. On the hop field the evaluation instances are copies of the
training instances up to relabelling, and the two columns coincide.}
\label{tab:fit}
\input{../figures/out/tab-fit.tex}
\end{table}

\paragraph{Fixed-reach baselines}
Table~\ref{tab:fixed} reports the two first-draft operators of~\eqref{eq:fixed}, with the constant
predictor beside them, and Table~\ref{tab:ladder} the full ladder at 1000 epochs. The fixed-reach
operators carry no learned $t$, so a spectral kernel that beats the best of them does so by
something other than reach alone. At 200 epochs they sit on the same floor as everything else on
the large shells, gaining at most \numFixedLargeMaxGainPct\% over the constant predictor there. At
1000 epochs the first-draft operators, whose measured reach is \numReachPprRoundOneShellOne{} to
\numReachSgcEightShellOne{} hops per layer, gain \numFixedGainPprHopsSmall\% (PPR) and
\numFixedGainSgcHopsSmall\% (SGC) over the constant predictor on the 264-satellite hop field,
\numFixedGainPprHopsShellOne\% and \numFixedGainSgcHopsShellOne\% at 1584 satellites and
\numFixedGainPprHopsDouble\% and \numFixedGainSgcHopsDouble\% at 3168. The 1584-satellite cell
is not explained by reach, since the 3168-satellite shell has the larger eccentricity and the
same operators do better there; it is the seed bimodality of Section~\ref{sec:hidden} again.
Of the \numFixedSgcHopsShellOneN{} valid SGC seeds in that cell, \numFixedSgcHopsShellOneAtConst{}
sit on the constant solution and \numFixedSgcHopsShellOneEscaped{} escaped it, which is what the
standard deviation of \numFixedSgcHopsShellOneSd{}, the largest in the grid, records; the PPR
cell splits \numFixedPprHopsShellOneAtConst{} to \numFixedPprHopsShellOneEscaped{}. A margin
measured against that cell, as the first draft's \numFixedGapPctHopsShellOne\% was, is a margin
over a baseline that half-failed to train, and it is withdrawn.

The ladder replaces it. Its best rung per (task, shell), chosen on the same ten seeds the kernels
are scored on and therefore favourable to the baselines, is \numLadderBestNameHopsSmall{} at 264
satellites on the hop field, \numLadderBestNameHopsShellOne{} at 1584 and
\numLadderBestNameHopsDouble{} at 3168, with errors \numLadderBestHopsSmall{},
\numLadderBestHopsShellOne{} and \numLadderBestHopsDouble{}; on the delay field
\numLadderBestNameDelaySmall{}, \numLadderBestNameDelayShellOne{} and
\numLadderBestNameDelayDouble{}. Against these, the better spectral kernel in its best $t$ arm
is ahead in all \numSpecBestBeatsLadderCells{} task-scale cells, by \numLadderGapBestPctHopsSmall\%,
\numLadderGapBestPctHopsShellOne\% and \numLadderGapBestPctHopsDouble\% on the hop field and
\numLadderGapBestPctDelaySmall\%, \numLadderGapBestPctDelayShellOne\% and
\numLadderGapBestPctDelayDouble\% on the delay field; in its worst $t$ arm it is behind the best
rung in every cell but one (\numLadderGapWorstPctHopsShellOne\% at 1584 and
\numLadderGapWorstPctHopsDouble\% at 3168 satellites on the hop field, a negative margin being a
loss), and both kernels beat the best rung in every arm in \numBothBeatLadderCells{} of the six
cells. The ladder has its own floor: SGC at $K'=64$, whose per-layer reach of
\numReachSgcSixtyFourSmall{} hops on the 264-satellite shell is a third of the eccentricity,
returns the constant predictor's error on both tasks there, the over-smoothing that a fixed reach
beyond the field's scale produces. What a learned propagation time buys is therefore a margin of
one to sixteen percent over a tuned fixed reach when its parameterisation and initialisation are
right, and a loss to it when they are not; the first draft's ``53\%'' was the untuned baseline's
failure, not the kernel's success.

\begin{table}[htbp]
\centering
\caption{The first-draft fixed-reach baselines of~\eqref{eq:fixed}: the constant predictor's
MAE and the MAE of personalised-PageRank propagation ($\alpha=\numPprAlpha$, $K=\numPprK$ steps)
and of the $K'=\numSgcK$ power of the normalised adjacency, mean $\pm$ sd over valid seeds (at
200 epochs all ten seeds are valid; at 1000 epochs the count is in parentheses), submitted
capacity, beside the lower of the two spectral kernels' means at 1000 epochs.}
\label{tab:fixed}
\input{../figures/out/tab-fixed.tex}
\end{table}

\begin{table}[htbp]
\centering
\caption{The baseline ladder at 1000 epochs, submitted capacity: MAE (mean over valid seeds,
count in parentheses) of SGC at four powers and of exact personalised PageRank at four
restart probabilities, whose measured reach is in Table~\ref{tab:reach}; the best rung per row,
which includes the first-draft baselines of Table~\ref{tab:fixed} in the competition; and the
lower of the two spectral kernels' means in its worst and its best $t$ arm.}
\label{tab:ladder}
\input{../figures/out/tab-ladder.tex}
\end{table}

\subsection{The majority verdict moves with the metric}
\label{sec:swap}

The preliminary version also reported the area under the error curve restricted to far nodes,
those beyond half the destination's eccentricity, as the metric on which a longer reach should
show most clearly. Formally, with $h(i,d)$ the hop distance of node $i$ from the destination and
$\epsilon$ the destination's eccentricity, the far-node error is the mean absolute error over
$\{i : h(i,d) > \epsilon/2\}$; it is in the same normalised units as the MAE and is larger because
the far nodes are the hard ones. Table~\ref{tab:aurc} reports it for grid 2 under the same rules,
with paired inference in Tables~\ref{tab:aurcinf} and~\ref{tab:pooledaurc}
(Appendix~\ref{app:tables}), and Table~\ref{tab:swap} sets the seed votes on the two metrics side
by side for every cell.

\begin{table}[htbp]
\centering
\caption{Grid 2, far-node error at 1000 epochs, submitted capacity: the constant predictor's
far-node error, then mean $\pm$ sd over valid seeds with the count in parentheses for each
operator, the operator leading in mean, the seed votes, and the majority verdict of rule~4
applied to this metric with the floor of rule~3 computed against the far-node constant.}
\label{tab:aurc}
\input{../figures/out/tab5-aurc.tex}
\end{table}



\begin{table}[htbp]
\centering
\caption{The same runs scored on two metrics. For each arm-level cell, the seed votes
heat:walk on MAE and on far-node error, how many votes moved, and the majority verdicts on the
two metrics.}
\label{tab:swap}
\input{../figures/out/tab-swap.tex}
\end{table}

On the far-node metric the pooled test certifies the same two cells as on MAE, the heat kernel
on the 264-satellite shell on both tasks (\numPooledSigHolmAurc{} of 6 after Holm), and no
direction on the larger shells; \numCIExclZeroAurc{} per-cell intervals exclude zero,
\numCIExclZeroHeatAurc{} favouring the heat kernel. Under the majority rule the heat kernel wins
in every arm at \numAurcHeatRobust{} of \numGthreeCells{} task-scale cells, against
\numGthreeHeatRobust{} on MAE; \numAurcSigRaw{} cells reach $p<0.05$ before per-cell correction
and \numAurcSigHolm{} after. The same runs, scored on the two metrics, move the seed vote by a
median of \numSwapMedianMoved{} and up to \numSwapMaxMoved{} seeds, and by two or more in
\numSwapCellsMovedTwoPlus{} of \numSwapCells{} cells. That is the
noise amplitude of the majority statistic, measured on the data themselves, and it is the size of
the margins by which the majority verdicts of Tables~\ref{tab:s264} and~\ref{tab:grid3} were
declared. On the metric chosen to favour reach, the operator built for reach does not win; but
neither, under inference, does the other.

\subsection{The seam-cut graph}
\label{sec:seam}

Every run above is on the torus. The seam-cut graph removes the inter-plane links between the
last and the first plane, so that $2S$ satellites have degree three, the graph is no longer
vertex-transitive, and a destination's hop field depends on where it sits relative to the seam.
It is the graph both versions of this study said they used. Grid~2 is repeated on it at 1000
epochs, submitted capacity, in \numSeamArms{} $t$ arms with the three operators
(\numSeamCoreRuns{} runs, of which \numSeamPathology{}, \numSeamPathologyPct\%, are excluded under
rule~2) and with the first-draft fixed-reach baselines (\numSeamFixedRuns{} runs), and read by the
same six rules. Table~\ref{tab:seam} sets the softplus, $t_{0}=0.5$ arm on the two graphs side by
side; Tables~\ref{tab:seamgrid} and~\ref{tab:seampooled} give every arm and the pooled test.

Two things change and one does not. First, the instances now differ: training and test error
separate by up to \numSeamFitMaxGap{}, and the GCN's seed dispersion, which was
\numGcnSdSmall{} or less on the torus, grows to the level of the spectral kernels'. Second, the
hop field at 1584 satellites moves. In the softplus, $t_{0}=0.5$ arm the walk kernel reaches
\numSeamHopsShellOneWalk{} against \numSeamHopsShellOneHeat{} for the heat kernel, a
\numSeamHopsShellOneWins{} seed vote at Wilcoxon $p=\numSeamHopsShellOnePW$; pooled over the
\numSeamArms{} arms the vote is \numSeamPooledHopsShellOneWins{} with
$\bar d=\numSeamPooledHopsShellOneD$ and corrected $p=\numSeamPooledHopsShellOneHolm$, and the
walk kernel wins in every arm under the majority rule at \numSeamWalkRobust{} of the six
task-scale cells, the heat kernel at \numSeamHeatRobust{}. The preliminary version's direction at
1584 satellites, which the torus does not reproduce, is therefore recovered on the graph the
preliminary version described, on its task, in \numSeamPooledWalkSig{} of the six pooled tests
after correction. What does not change is the rest of the grid: the pooled leaders agree with
the torus in \numSeamPooledLeaderAgree{} of six (task, shell) pairs, the heat kernel keeps the
264-satellite shell on both tasks (corrected $p=\numSeamPooledHopsSmallHolm$ and
$\numSeamPooledDelaySmallHolm$), and the delay field certifies no direction on the large shells
(\numSeamPooledDelayShellOneWins{} and \numSeamPooledDelayDoubleWins{}, corrected
$p=\numSeamPooledDelayShellOneHolm$ and $\numSeamPooledDelayDoubleHolm$). The far-node metric
(Table~\ref{tab:seampooledaurc}) certifies \numSeamPooledSigHolmAurc{} of six.

The reading is not that the walk kernel is the better operator on seam-cut graphs. It is that a
single link pattern, the seam, moves one cell of the grid from ``no direction'' to ``walk'' at
the corrected level, on one task, while the other five cells stay where they were; an operator
ranking on these graphs is a property of the graph family as much as of the operators, and the
preliminary version, which reported the seam-cut result without having run it, was right about
the direction at that cell for the wrong reason. The learned $t$ tells the same story from the
other side: on the seam-cut graph the heat kernel's first-layer $t$ at 1584 satellites is
\numSeamTlayerOneShellOneHeat{} against \numTlayerOneShellOneHeat{} on the torus, and the walk
kernel's \numSeamTlayerOneShellOneWalk{} against \numTlayerOneShellOneWalk{}.

\begin{table}[htbp]
\centering
\caption{Torus and seam-cut graph side by side, 1000 epochs, submitted capacity, softplus arm
with $t_{0}=0.5$: the constant predictor, the three operators (mean $\pm$ sd over valid seeds,
count in parentheses), the two first-draft fixed-reach baselines, the seed vote, the Wilcoxon
$p$ and the operator leading in mean.}
\label{tab:seam}
\input{../figures/out/tab-seam.tex}
\end{table}


\begin{table}[htbp]
\centering
\caption{Seam-cut graph, MAE: the pooled form of rule~6, one test per (task, shell), with the
columns of Table~\ref{tab:pooled}.}
\label{tab:seampooled}
\input{../figures/out/tab-seam-pooled.tex}
\end{table}


\subsection{Sensitivity of the reading rules}
\label{sec:sens}

Table~\ref{tab:sens} repeats the two rule-dependent counts of this paper at other values of the
rules: the number of task-scale cells in which each kernel wins in every arm under the majority
rule, at floors of 1\%, 2\%, 5\% and 10\% and at majorities of six, seven and eight of ten; and
the number of large-shell hop-field cells of grid 1 that are rankable at each floor. The
statement that no operator learns on the large shells at 200 epochs holds at 5\% and 10\%, weakens
to \numRankableLargeAtGTwo{} of 24 cells at 2\% and \numRankableLargeAtGOne{} at 1\%, all on the
1584-satellite shell. The left block of the table is flat in the floor because every grid-2 cell
clears even a 10\% floor, and its one movement is in the majority threshold: at eight of ten the
two heat-majority cells become \numRobustHeatAtMajEight{} heat and \numRobustWalkAtMajEight{}
walk, so the only majority result that survives rule~5 disappears under a one-seed tightening of
the vote. The pooled inference of Section~\ref{sec:budget} depends on neither threshold.

\begin{table}[htbp]
\centering
\caption{Sensitivity of the rule-dependent counts to the floor threshold $g$ and the majority
threshold. Left: task-scale cells of grid 2 in which the heat / walk kernel wins in every arm
under rule~4 at the given majority. Right: hop-field cells of grid 1 at 1584 satellites and above
that are rankable at the given floor.}
\label{tab:sens}
\input{../figures/out/tab-sens.tex}
\end{table}

\subsection{Long training collapses a tenth of the runs}
\label{sec:collapse}

Of the \numGridThreeRuns{} runs of grid 2, \numGridThreePathology{}
(\numGridThreePathologyPct\%) were excluded under rule~2. Inspection shows one pattern: the loss
falls, then rises again by up to \numGridThreeRiseMaxPct\%, and the run ends on the constant
solution. Table~\ref{tab:collapse} breaks the count down by operator and records whether each
excluded run had escaped the constant solution before collapsing: the GCN collapses in
\numCollapseGcn{} runs (\numCollapseGcnEscaped{} of them escapers), the heat kernel in
\numCollapseHeat{} (\numCollapseHeatEscaped{}) and the walk kernel in \numCollapseWalk{}
(\numCollapseWalkEscaped{}); the fixed-reach baselines of~\eqref{eq:fixed} collapse in
\numCollapsePpr{} (PPR) and \numCollapseSgc{} (SGC) runs. The instability is not specific to the
spectral operators or to the learned $t$, and because most excluded runs had not escaped, exclusion removes non-escaping runs
preferentially and every surviving mean is biased toward the escapers; the GCN, with the highest
exclusion rate, is biased most. The paired tests of Section~\ref{sec:budget} are computed on the
seeds valid for both kernels, so a seed removed for one kernel is removed for both. A fixed
learning rate that is adequate for 200 epochs is not stable for 1000 on these graphs; we report
the rate rather than tune it away.

\begin{table}[htbp]
\centering
\caption{Runs excluded as pathologies (loss rising by more than 1\% between thirds) out of
runs, 1000 epochs, submitted capacity, both tasks and all $t$ arms pooled, and whether the
excluded run had escaped the constant solution (MAE below 90\% of the constant predictor) before
collapsing.}
\label{tab:collapse}
\input{../figures/out/tab6-collapse.tex}
\end{table}

\paragraph{Which runs collapse depends on the floating-point precision}
The eigenpairs are cached in single precision, and the first draft's check that cached and
recomputed eigenpairs agree was a check of determinism, not of adequacy. The direct test is to
rerun a cell with every tensor in double precision. Table~\ref{tab:precision} does so for the
hop field at 1584 satellites, softplus arm with $t_{0}=0.5$, all ten seeds and all three
operators (\numPrecRuns{} runs). The GCN's per-seed error moves by at most
\numPrecGcnDelta{}. The two kernels' per-seed errors move by up to \numPrecHeatDelta{} (heat)
and \numPrecWalkDelta{} (walk), which is the distance between the escaped and the constant
solution: the same seed, with the same initial weights and the same instances, escapes in one
precision and collapses in the other, and \numPrecPathologyThirtyTwo{} runs are excluded as
pathologies in single precision against \numPrecPathologySixtyFour{} in double, on different
seeds. The mean paired difference $\bar e_{\mathrm{heat}}-\bar e_{\mathrm{walk}}$ in this cell is
\numPrecDiffThirtyTwo{} in single precision and \numPrecDiffSixtyFour{} in double, with a
\numPrecWinsSixtyFour{} vote in double. A training trajectory that a change of rounding can move
from one basin to the other is chaotic in the practical sense, and for the spectral kernels at
this scale the precision acts as one more random seed. It does not affect all arms alike, as the
first draft's threat statement assumed; it affects each run separately, and the seed-level
bimodality of Section~\ref{sec:hidden} is its visible form.

\begin{table}[htbp]
\centering
\caption{The same cell in single and double precision: hop field, 1584 satellites, 1000 epochs,
submitted capacity, softplus arm with $t_{0}=0.5$. Mean MAE over the ten seeds (pathological runs
included, since which runs are pathological is part of the result) in each precision, and the
largest change of a single seed's MAE between the two.}
\label{tab:precision}
\input{../figures/out/tab-precision.tex}
\end{table}


\subsection{Cost, and the scale at which the method is feasible}
\label{sec:cost}

Table~\ref{tab:cost} reports the median wall-clock time per run on one consumer GPU with 24~GB
of memory, with the eigendecomposition of each instance cached. The spectral operators build an
$N\times N$ propagator per layer per instance per epoch from the eigenbasis, an $O(N^{3})$ product
that dominates at scale; the walk kernel builds two such products, for the real and imaginary
parts, and squares them. On the 4400-satellite shell at 200 epochs a walk run takes
\numSecsWalkLarge{}~s against \numSecsHeatLarge{}~s for the heat kernel and \numSecsGcnLarge{}~s
for the GCN, whose propagator is sparse. On CPU, before caching, the same cell took
\numCpuSecsLarge{}~s, which is why the preliminary version stopped at 1584 satellites. As
implemented, with dense $N\times N$ single-precision matrices for the eigenbasis, the propagator
and their gradients, the method fits in 24~GB up to roughly $N=\numFeasibleNGpuMem{}$ nodes;
beyond that it needs a factorised or sparse propagator, which is a different implementation. The
mega-constellations of the introduction lie at the edge of that range and beyond it. For a
comparison across scales, the cost of the operator is part of what is being compared.

\begin{table}[htbp]
\centering
\caption{Median wall-clock seconds per training run on one GPU (eigendecomposition cached),
submitted capacity, by shell and operator, at the two budgets. The 4400-satellite shell was not
run at 1000 epochs.}
\label{tab:cost}
\input{../figures/out/tab8-cost.tex}
\end{table}

% =====================================================================
\section{Discussion}
\label{sec:discussion}

\paragraph{What was tested, and what was not}
The object of this audit is the transition-probability kernel~\eqref{eq:walk}. It is a real
nonnegative row-stochastic matrix, exactly as the heat kernel is, and applying it to features
damps and mixes them as a diffusion step does; what it inherits from the walk is the shape of its
rows, an oscillatory profile whose width grows linearly with $t$ rather than as a square root
(Figure~\ref{fig:spreading}). Once $t$ is a trained parameter, the heat kernel reaches as far by
learning a larger $t$, which the right panel of Figure~\ref{fig:tlearned} and
Table~\ref{tab:reach} show it does, in the first layer and in proportion to the shell. What the
paired tests find has to be stated in two halves. Where the design has power, on the
264-satellite shell, the heat kernel is better on both tasks, on both graphs, in every arm
(Tables~\ref{tab:pooled} and~\ref{tab:seampooled}); \numCIExclZeroSmall{} of the
\numCIExclZero{} per-cell intervals that exclude zero are on that shell, and they point the same
way. On the larger shells of the torus the tests certify nothing, and the reason is stated in
MAE units: the minimum detectable effect there is \numPooledMdeLargeMin{} to
\numPooledMdeMax{}, and the observed differences are smaller. That is absence of evidence, not
evidence of absence, and the first draft's sentence that the kernels ``are indistinguishable''
overstated a null in the same way that the preliminary version overstated a difference. On the
seam-cut graph the walk kernel takes the 1584-satellite hop field at the corrected level. The
argument of CTQW-GNN~\cite{ctqwgnn} for the unitary propagator in feature space, which damps no
frequency and preserves feature norm, is a different object and is neither confirmed nor refuted
here.

\paragraph{What the floor means for learned propagation on constellations}
A budget that suffices on a 264-satellite shell does not on a 1584-satellite one, and the failure
is silent: the run reports a finite, decreasing loss and an error that looks like a number.
Carrying the constant baseline beside every result is necessary and not sufficient: at 200
epochs the gains over it on the large shells were positive in every cell
(Table~\ref{tab:floor}), and what exposed the floor was a threshold on the gain, whose value this
study set with the 4400-satellite cells in view (Section~\ref{sec:rules}). The operative safeguard
is a gain threshold fixed and justified before the runs, and a budget that is shown to clear it
on every shell; the present study can only report that its own threshold was post hoc and that
its conclusions hold at four values of it.

\paragraph{What the seed-level bimodality and the vote noise mean}
The split in Figure~\ref{fig:arms}, with escape rates of \numEscapeHeatPct\% and
\numEscapeWalkPct\% ($p=\numEscapePz$), means that at the preliminary budget a majority verdict
counts which seeds happened to escape. At the larger budget the majority statistic still moves by
up to \numSwapMaxMoved{} seeds between two correlated metrics on the same runs
(Section~\ref{sec:swap}), which is the margin by which most of its verdicts were declared. The
preliminary version's \numPrelimSeeds{}-seed result at 1584 satellites sits inside the range that
this bimodality produces on the torus, where ten seeds split \numPooledHopsShellOneWins{}; on
the seam-cut graph the same cell splits \numSeamPooledHopsShellOneWins{} in the preliminary
direction. The bimodality itself is not a property of the seeds: Section~\ref{sec:collapse}
shows that the precision of the arithmetic moves a seed between the two modes, so the split is
a property of the training dynamics at this scale, and the number of seeds only measures it.

\paragraph{Consequences for reporting}
These are not new rules, and they are not the contribution; they are what this audit needed to
avoid repeating its own preliminary version, and two of them are what its own first draft needed
and lacked. Carry the constant predictor beside every result, on every metric. Let the budget grow
with the graph, or show that it need not. Treat any learned reach parameter's parameterisation and
initialisation as an arm. Measure the reach of a ``reach-matched'' baseline. Use a paired test
with a multiplicity correction, state the false-positive rate of any vote rule reported beside
it, and state the minimum effect the corrected test can detect, since a null from a test that
could not reject is not a finding. Count collapsed runs and report whether they had escaped. Check
that the graph the code builds is the graph the text describes, by a property the text can be
wrong about, such as the degree sequence.

\paragraph{What would change the verdict}
A propagator applied in feature space, unitary rather than stochastic, would test the argument of
CTQW-GNN~\cite{ctqwgnn} directly; the wave-type models PDE-GCN~\cite{pdegcn} and graph-coupled
oscillator networks~\cite{graphcon} are the classical form of the same argument and were not run.
A learning-rate schedule would reduce the collapse rate of Section~\ref{sec:collapse}, and with
it the chaotic sensitivity to precision, and might decide cells that are now undecided; more
seeds would lower the minimum detectable effect of the pooled tests, which at 1584 satellites on
the hop field is \numPooledHopsShellOneMde{} with ten. Other link patterns than the torus and the
seam cut, and the time-varying links of a real constellation, would say how far the
seam-cut result of Section~\ref{sec:seam} generalises. A task with load, in which the field to be predicted depends
on traffic and not only on geometry, and a time-varying evaluation that exercises the link
churn the introduction invokes, would say whether any operator ranking survives contact with a
network function. None of these is a small addition, and the present paper claims nothing about
them.

% =====================================================================
\section{Threats to validity}
\label{sec:threats}

\textbf{The two largest shells are synthetic, and all graphs are static.} The 3168- and
4400-satellite graphs scale the Starlink shell-1 geometry and are not deployed constellations;
they serve to grow the diameter. Every instance is an independent snapshot, so the time-varying
property that motivates learned propagation on these graphs is never exercised.

\textbf{The tasks are node fields, not network functions.} Hop and delay fields isolate
propagation from load, which is what the comparison needs, at the cost of distance from any
routing or traffic-engineering objective. The findings bear on the evaluation of propagation
operators, not on the routing or resource-management systems that might use them.

\textbf{The floor threshold was chosen with the data in view.} Rule~3's value was set after the
4400-satellite cells were seen, as Section~\ref{sec:rules} states; Table~\ref{tab:sens} reports
every dependent count at four values of it.

\textbf{The majority rule has a 34.4\% null false-positive rate, and the per-cell test has
almost no power.} The majority rule is kept only so that the preliminary verdicts are
reproducible, and no conclusion rests on it. The per-cell Holm-corrected Wilcoxon test that the
first draft stood on cannot reject in \numPowerCannotReject{} of \numGthreeArmCells{} cells at
any effect size (Table~\ref{tab:power}); it is reported as description. The pooled test the
paper now stands on has ten seeds per test and a minimum detectable effect that is stated beside
every result; on the large shells it is \numPooledMdeLargeMin{} to \numPooledMdeMax{} in MAE
units, which is coarse.

\textbf{The baseline ladder is a ladder, not a tuning.} $\alpha$ and $K'$ take four values each,
chosen to bracket the measured reach of the kernels, and the best rung is selected on the same
seeds the comparison is scored on, which favours the baselines. A finer search might find a
better fixed reach; it might also find a worse one, since the ladder's top rung already
over-smooths the small shell.

\textbf{Exclusion is differential.} Rule~2 removes runs after they have collapsed to the constant
solution, so it removes non-escapers preferentially, and the GCN loses more runs than the spectral
kernels (Table~\ref{tab:collapse}). Means are therefore optimistic, most for the GCN.

\textbf{One learning rate, one optimiser, six evaluation instances per seed.} The collapse rate is
specific to Adam at 0.01 for 1000 epochs. Each seed evaluates on its own six instances, so the
sixty instances of a cell are distinct, but instance-level dispersion within a seed is not
reported.

\textbf{The arithmetic is single precision, and the trajectories are chaotic.} Every arm
shares one single-precision eigendecomposition per instance. Section~\ref{sec:collapse} shows
that rerunning a cell in double precision moves individual seeds between the escaped and the
collapsed mode and changes the sign of the cell's mean paired difference. The comparison between
kernels within a precision is fair, since both see the same rounding; but any single run's
outcome at 1584 satellites and above is not reproducible across precisions, and a result that
rests on a handful of seeds at that scale, including the preliminary version's, is a sample from
this chaos.

\textbf{Several references are preprints.} CTQW-GNN~\cite{ctqwgnn}, CTQWformer~\cite{ctqwformer}
and the LEO graph-learning work of Gu et al.~\cite{deepladu} are unrefereed at the time of
writing, and GraphQWalk~\cite{graphqwalk} is a recent journal article. The argument this audit
declines to test rests on the first of these; the audit itself rests on the preliminary version,
which is not in the refereed record either.

\textbf{The preliminary artifact does not reproduce its own headline, and both versions
misdescribed their graph.} The released results of the preliminary version record
\numPrelimParams{} parameters for the 1584-satellite experiment, which corresponds to the
width-32, four-layer network its text describes, while the released code defaults to width 16
and three layers (\numReleasedParams{} parameters) and contains no script for that experiment.
Both the preliminary version and the first drafts of this paper state that the counter-rotating
seam is cut. In the generator the flag \texttt{seam=True} keeps the seam links, and every run of
the two main grids was made with it; a docstring written for this study read the flag the other
way, and the text copied the docstring. The error was found by reading Table~\ref{tab:fit}, where
training and test error coincide, and confirmed by the degree sequence: every node of every
main-grid snapshot has degree four. The main grids are therefore on the torus, described as such
throughout this version, and the seam-cut graph is run as its own arm
(Section~\ref{sec:seam}). The preliminary version's 1584-satellite claim was, in the event, made on
the torus and recovered here on the seam-cut graph; neither its text nor its authors knew which
graph it was on.

% =====================================================================
\section{Conclusion}
\label{sec:conclusion}

We audited a preliminary result of our own: that the transition-probability kernel of a
continuous-time quantum walk outperformed the heat kernel on a 1584-satellite ISL graph and did so
increasingly with scale. Under the preliminary budget the large shells are a floor on which no
operator learns; at the one shell where learning occurs the ranking follows the parameterisation
and initialisation of the learned propagation time through a seed-level bimodality with
indistinguishable escape rates. With a budget that clears the floor, the picture depends on the
graph and on the question the test can answer: on the torus, which is the graph every main-grid
run was in fact made on, the heat kernel is better on the smallest shell on both tasks and no
direction is certified on the larger ones, where the minimum detectable effect of a ten-seed
paired test is larger than any difference observed; on the seam-cut graph, which both versions
described, the preliminary direction at 1584 satellites on the hop field is recovered at the
corrected level, and nothing else moves. The two kernels are the same kind of object, a real
row-stochastic propagator with a learned reach; what the learned reach buys against a fixed reach
tuned over a ladder is \numLadderGapBestPctHopsShellOne\% at 1584 satellites in the best $t$ arm
and a loss in the worst. What the audit adds beyond the correction of its own report is measured:
a silent budget floor that scales with the graph, a propagation-time parameterisation that
decides rankings at small budgets, a vote statistic whose noise amplitude is two to three seeds, a
per-cell corrected test that could not have rejected in three quarters of its cells, a
``reach-matched'' baseline that reached half as far as the kernels it was matched to, a
misread flag that put every run on a different graph from the one described, and a training
dynamics chaotic enough that the arithmetic's precision decides which seeds collapse. Each is a
reason to distrust a small-seed operator comparison on constellation graphs, including the one
we published first and the one we drafted second.

\appendix
\section{Supplementary tables}
\label{app:tables}
The per-cell inference on the far-node metric (Table~\ref{tab:aurcinf}), the pooled tests on that
metric for both graphs (Tables~\ref{tab:pooledaurc} and~\ref{tab:seampooledaurc}), and the
seam-cut grid arm by arm (Tables~\ref{tab:seamgrid} and~\ref{tab:seamgridinf}) are collected
here; every number in them is
produced by the same script as the tables of the main text.

\begin{table}[H]
\centering
\caption{Grid 2, far-node error at 1000 epochs, submitted capacity: paired inference across
seeds for each arm-level cell, with the same columns as Table~\ref{tab:grid3inf}: mean per-seed
difference $e_{\mathrm{heat}}-e_{\mathrm{walk}}$ on the far-node metric with its 95\% bootstrap
interval, exact sign-test $p$, exact Wilcoxon $p$, and the Wilcoxon $p$ Holm-corrected over the
\numGthreeArmCells{} cells of this table; the constant predictor for each cell is in
Table~\ref{tab:aurc}. The pooled form of rule~6 on this metric is in Table~\ref{tab:pooledaurc}.}
\label{tab:aurcinf}
\input{../figures/out/tab5b-aurc-inference.tex}
\end{table}

\begin{table}[H]
\centering
\caption{Grid 2, far-node error: the pooled form of rule~6, one test per (task, shell), with
the columns of Table~\ref{tab:pooled}.}
\label{tab:pooledaurc}
\input{../figures/out/tab-pooled-aurc.tex}
\end{table}

\begin{table}[H]
\centering
\caption{Seam-cut graph, MAE at 1000 epochs, submitted capacity, every $t$ arm, with the columns
of Table~\ref{tab:grid3}.}
\label{tab:seamgrid}
\input{../figures/out/tab-seam-grid.tex}
\end{table}

\begin{table}[H]
\centering
\caption{Seam-cut graph, MAE: paired inference across seeds for each arm-level cell, with the
columns of Table~\ref{tab:grid3inf}; Holm correction over the \numSeamArmCells{} cells of this
table.}
\label{tab:seamgridinf}
\input{../figures/out/tab-seam-grid-inference.tex}
\end{table}

\begin{table}[H]
\centering
\caption{Seam-cut graph, far-node error: the pooled form of rule~6.}
\label{tab:seampooledaurc}
\input{../figures/out/tab-seam-pooled-aurc.tex}
\end{table}

\section*{Data and code availability}
All code, the cached graph instances' construction, every result file of the
\numTotalRuns{} main-grid and \numRevTwoRuns{} auxiliary runs, the scripts that produce every figure, table
and number in this paper, and the dated protocol declarations are available at
\url{https://github.com/haodpsut/leo-propagation-reach}. The preliminary version and its
released artifact are available at \url{https://github.com/haodpsut/qi-gnn-dynamic-wireless}.

\section*{CRediT authorship contribution statement}
\textbf{Phuc Hao Do:} Conceptualization, Methodology, Software, Investigation, Data curation,
Writing, Visualization.

\section*{Declaration of competing interest}
The author declares no competing interests.

\section*{Declaration of generative AI use}
During the preparation of this work the author used a large language model (Claude, Anthropic)
to assist with code, data analysis scripts and editing of the text. After using this tool, the
author reviewed and edited the content as needed and takes full responsibility for the content
of the publication.

\bibliographystyle{elsarticle-num}
\bibliography{refs}

\end{document}
